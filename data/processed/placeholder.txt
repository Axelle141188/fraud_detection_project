This directory contains processed and transformed datasets ready for modeling.
It includes cleaned data, feature-engineered datasets, and train/test splits.
These processed files are the direct inputs for the machine learning models in the project.
